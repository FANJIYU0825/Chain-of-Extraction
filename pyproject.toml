[project]
name = "Chain-of-Extraction"
version = "0.1.0"
description = "Here is a chain extraction of llm"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.6.7",
    "pandas>=2.3.2",
]
